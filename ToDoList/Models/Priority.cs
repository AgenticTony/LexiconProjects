namespace ToDoList.Models
{
    // Enumeration to define task priority levels
    public enum Priority
    {
        Low = 1,     // Low importance tasks
        Medium = 2,  // Normal priority tasks
        High = 3     // Urgent/important tasks
    }
}